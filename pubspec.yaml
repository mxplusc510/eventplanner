name: eventplanner
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  #calendar
  syncfusion_flutter_calendar: ^21.1.38
  #state management
  provider: ^6.0.5
  #handle dates
  intl: ^0.17.0

  event_calendar: ^0.0.9
  share: ^2.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
